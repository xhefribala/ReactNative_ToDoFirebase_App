import styled from "styled-components";
import { View } from "react-native";

// colors used during application
export const Colors = {
  primary: "",
  secondary: "",
  tertiary: "",
  textColour: "#2596rebe",
  white: "#fff",
  backColour: "#eeeee4",
};

const { primary, secondary, tertiary, textColour, white, backColour } = Colors;
